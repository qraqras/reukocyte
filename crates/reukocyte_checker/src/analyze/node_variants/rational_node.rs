#![allow(unused_variables)]
use crate::checker::Checker;
use ruby_prism::RationalNode;

/// Run lint rules over a [`RationalNode`] syntax node.
pub(crate) fn rational_node(node: &RationalNode, checker: &mut Checker) {
    // TODO: Add rules for RationalNode
}
